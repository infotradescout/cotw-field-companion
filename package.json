{
  "name": "cotw-field-companion",
  "version": "0.3.0",
  "private": true,
  "description": "Local, read-only COTW field journal and save observer. Private validation build.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node launcher.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}

{
  "name": "cotw-field-companion",
  "version": "0.4.1",
  "private": true,
  "description": "GrindZone — an unofficial theHunter: Call of the Wild companion with public reference tools and a private local save observer.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node launcher.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:web": "node tools/build-web.mjs"
  }
}

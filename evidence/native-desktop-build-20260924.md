# GrindZone native desktop candidate — 2026-09-24

- Source branch: `fix/grindzone-native-window-20260924`, based on exact `ea4214c3d60011b4f1ae201d39d90fc157f329d5`. The owner installation under `%LOCALAPPDATA%\GrindZone` was not changed for this candidate.
- The installed shortcut still targets `START.cmd`. The proposed signed installer repairs that launcher to request `boot --desktop`, and the signed supervisor starts `desktop/GrindZone.Desktop.exe` after the selected server commits. Desktop-window exit requests a graceful `runtime.stop()`. The managed path does not open the default browser.
- The WinForms host embeds Microsoft Edge WebView2. It accepts only a loopback URL and keeps its WebView2 profile under the installation home, separate from the COTW journal and game saves. Owner PC has .NET Framework release registry value `533509`; WebView2 Runtime is present and loaded during the smoke test.
- Pinned build inputs: .NET SDK `8.0.425`; Microsoft.Web.WebView2 `1.0.4191.47`; Microsoft.NETFramework.ReferenceAssemblies.net48 `1.0.3`; locked NuGet content hashes in `desktop/packages.lock.json`. The build-only Linux SDK archive SHA-512 is `934b8060a7190e5909ad1fd0785db542f487b3bbf6cdd14826b02095fdd0d0394298b1634085eff302928fccc33f7c1a7253e9b87df555fc36fce819bcd2e798`.
- Windows build: six x64 desktop files produced by `tools/build-desktop.mjs` with SDK in an isolated build-tools folder. Ubuntu WSL build: official Linux SDK archive downloaded under `/tmp`, SHA-512 verified, locked `net48` WinForms/WebView2 publish passed, and the same source build tool returned six Windows x64 files. The source build tool now downloads that pinned SDK into a temporary build directory on Linux when no matching SDK is provided; it verifies SHA-512 before extraction and sets `DOTNET_GENERATE_ASPNET_CERTIFICATE=false`.
- Windows UI smoke used a disposable localhost HTTP server and profile in the isolated build-tools folder. The native window requested `/`, its script requested `/loaded`, and the `close` stdin command exited the host with code 0. No browser tab or live journal was used.
- Focused source tests: 29/29 existing install, update, portable and Windows-download tests, including next-launch staged activation through a synthetic owned desktop window; 2/2 signed desktop-package and directed signer-declaration tests. These are local tests, not owner-installed acceptance.
- On the first local SDK run before the build tool suppressed certificate generation, .NET created a `CN=localhost` development certificate in `Cert:\CurrentUser\My`, thumbprint `C6C1040E03E8066A72F4837AC67BF8AE095916CD`, NotBefore `2026-09-23 23:50:55` local. It was not trusted. The exact new certificate was removed immediately; a later `Test-Path` returned false. No OS execution policy or trust setting was changed.
- Current Sway main `dc3436d41342f76dd53e5f07435ab08d094968ef` uses Render `runtime: node`, `buildCommand: npm ci && npm run build`, and a clean environment that does not pass `GRINDZONE_DOTNET`. The new GrindZone builder's pinned Linux SDK bootstrap makes this path buildable without a Sway build-command change, but the combined new GrindZone source still requires its own exact Sway download pin, signed hosted build, and owner-PC install proof.
